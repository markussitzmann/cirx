BEGIN;

 cir_pubchem_structure_name_cid


-- CREATE TABLE IF NOT EXISTS cir_pubchem_structure_name_cid(
--     id bigserial PRIMARY KEY,
--     name_id bigint NOT NULL,
--     cid bigint NOT NULL,
--     UNIQUE(name_id, cid)
-- );

-- create TABLE if not EXISTS cir_pubchem_sid_map(
--     id bigserial PRIMARY KEY,
--     cid bigint NOT NULL,
--     sid bigint NOT NULL,
--
-- )



COMMIT;



